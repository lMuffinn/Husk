/*
 * GameData.cs
 * Authors: Matthew Eagleman
 * Date Created: spring 2023
 * Purpose: Keep track of the floor the player is on, along with other data maybe. Might trash this one
 */
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameData : MonoBehaviour
{
    public int floor = 2;// I believe all this does is keep track of the floor the player is on? again, pretty bad
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
